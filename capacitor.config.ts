import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Grad_Project_Final',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
